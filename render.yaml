services:
  - type: web
    name: computervision-road-anomaly-detection
    env: python
    region: oregon
    plan: starter
    buildCommand: |
      apt-get update && apt-get install -y libgl1-mesa-glx libgthread-2.0-0
      pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
      - key: PORT
        value: 10000
      - key: WEB_CONCURRENCY
        value: 1
    healthCheckPath: /
    autoDeploy: true
    disk:
      name: uploads
      mountPath: /opt/render/project/src/static/uploads
      sizeGB: 1 